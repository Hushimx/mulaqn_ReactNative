# دليل البدء السريع - المرحلة الأولى 🚀

## ✅ ما تم إنجازه

تم تطوير **4 صفحات رئيسية** مع ربط كامل بـ Laravel API:

1. ✅ **صفحة المسار** - عرض جميع دروس المسار
2. ✅ **تفاصيل الدرس** - معلومات الدرس وإحصائيات التقدم
3. ✅ **حل الأسئلة** - واجهة تفاعلية للإجابة على الأسئلة
4. ✅ **تحديثات الصفحة الرئيسية** - ربط مع المسارات

---

## 🎯 كيفية التجربة

### 1. شغل Laravel Backend
```bash
cd /Users/osa/Desktop/Mulaqn_Laravel
php artisan serve
```

### 2. شغل Expo
```bash
cd /Users/osa/Desktop/Mulaqn_Laravel/mulaqn_ReactNative
npx expo start
```

### 3. غيّر IP Address للأندرويد
في ملف `utils/api.ts`:
```typescript
const API_BASE_URL = __DEV__ 
  ? 'http://192.168.X.X:8000/api'  // ضع IP جهازك هنا
  : 'https://your-production-domain.com/api';
```

**للحصول على IP جهازك:**
- **Mac/Linux**: `ifconfig | grep "inet " | grep -v 127.0.0.1`
- **Windows**: `ipconfig`

### 4. سجل دخول
```
Email: student@mulaqn.test
Password: password
```

---

## 📱 تجربة التدفق الكامل

### خطوة بخطوة:

1. **افتح التطبيق** → ستظهر الصفحة الرئيسية (Home)
   
2. **اضغط على أي مسار** (قدرات/تحصيلي/STEP)
   - ستنتقل لصفحة المسار
   - سترى قائمة الدروس
   - كل درس يظهر معه: الصعوبة، المدة، الحالة

3. **اضغط على أي درس**
   - ستنتقل لتفاصيل الدرس
   - سترى الوصف والمحتوى
   - إحصائياتك (إذا حليت قبل كذا)

4. **اضغط "ابدأ الدرس"**
   - سيبدأ محاولة جديدة
   - ستنتقل لصفحة الأسئلة

5. **أجب على الأسئلة**
   - اختر الإجابات
   - اضغط "التالي" للسؤال التالي
   - اضغط "تسليم" بعد الانتهاء

6. **شاهد النتيجة**
   - سيظهر Alert بنتيجتك
   - ارجع للدرس أو المسار

---

## 📂 الملفات الجديدة

```
mulaqn_ReactNative/
├── app/
│   ├── tracks/
│   │   └── [id].tsx              ← صفحة المسار (جديد)
│   ├── lessons/
│   │   ├── [id].tsx              ← تفاصيل الدرس (جديد)
│   │   └── attempt/
│   │       └── [id].tsx          ← حل الأسئلة (جديد)
│   └── (tabs)/
│       └── index.tsx             ← تم التحديث
├── locales/
│   └── ar.json                   ← تم إضافة نصوص جديدة
├── utils/
│   └── api.ts                    ← جاهز ومكتمل
├── PHASE1_IMPLEMENTATION.md      ← التوثيق الكامل (جديد)
└── QUICK_START_PHASE1.md         ← هذا الملف (جديد)
```

---

## 🎨 لقطات الشاشات المتوقعة

### 1. صفحة المسار
```
┌─────────────────────────────┐
│  ← [المسار]              🏠│
├─────────────────────────────┤
│                             │
│  📚 قدرات - QDR            │
│  وصف المسار...             │
│                             │
│  📊 الإحصائيات:            │
│  📖 15 درس   ✓ 5   📈 33%  │
│                             │
├─────────────────────────────┤
│  📋 قائمة الدروس:          │
│                             │
│  ┌───────────────────────┐  │
│  │ 1️⃣ التناظر اللفظي   │  │
│  │ 🟢 سهل  ⏱ 15 دقيقة  │  │
│  │ ⭐ 85%              →│  │
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │ 2️⃣ المنطق الرياضي   │  │
│  │ 🟠 متوسط ⏱ 20 دقيقة │  │
│  │                     →│  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

### 2. صفحة حل الأسئلة
```
┌─────────────────────────────┐
│  ✕      [3 من 5]         ✓ │
├─────────────────────────────┤
│  ████████░░░░░░░░░░░   60%  │
├─────────────────────────────┤
│                             │
│  ┌───────────────────────┐  │
│  │ 3️⃣ [اختيار واحد]    │  │
│  │                       │  │
│  │ ما هو ناتج: 5 + 3؟  │  │
│  └───────────────────────┘  │
│                             │
│  ⭘ 5                        │
│  ⭘ 6                        │
│  ⦿ 8                        │ ← محدد
│  ⭘ 10                       │
│                             │
├─────────────────────────────┤
│  [← السابق]      [التالي →]│
└─────────────────────────────┘
```

---

## 🔧 استكشاف الأخطاء

### مشكلة: "Network request failed"
**الحل**:
1. تأكد أن Laravel شغال على `http://localhost:8000`
2. تأكد أن IP Address صحيح في `utils/api.ts`
3. للأندرويد، استخدم IP الجهاز مو `localhost`

### مشكلة: "401 Unauthorized"
**الحل**:
1. سجل دخول من جديد
2. تأكد من صلاحية الـ Token
3. تأكد من وجود User في Database

### مشكلة: "لا توجد دروس"
**الحل**:
1. شغل الـ Seeders:
```bash
php artisan db:seed --class=QudratLessonsSeeder
```

---

## 📊 API Endpoints المستخدمة

### Public (بدون Auth):
- `GET /api/tracks` - قائمة المسارات
- `GET /api/tracks/{id}` - تفاصيل مسار

### Protected (تحتاج Auth):
- `GET /api/tracks/{id}/lessons` - دروس المسار
- `GET /api/lessons/{id}` - تفاصيل الدرس
- `POST /api/lesson-attempts` - بدء محاولة
- `POST /api/lesson-attempts/{id}/responses` - حفظ إجابة
- `POST /api/lesson-attempts/{id}/submit` - تسليم

---

## 🎉 كل شيء يشتغل؟

إذا كل شيء تمام، المفروض تشوف:
- ✅ المسارات الثلاثة في الصفحة الرئيسية
- ✅ قائمة الدروس عند الضغط على مسار
- ✅ تفاصيل الدرس عند الضغط على درس
- ✅ الأسئلة عند بدء الدرس
- ✅ النتيجة بعد التسليم

---

## 📞 دعم

إذا واجهت أي مشكلة:
1. تحقق من Console/Terminal للأخطاء
2. تأكد من تشغيل Laravel و Expo
3. راجع ملف `PHASE1_IMPLEMENTATION.md` للتفاصيل الكاملة

---

**بالتوفيق! 🚀**

